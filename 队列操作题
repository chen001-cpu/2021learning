根据输入的操作命令,操作队列(1)入队、(2)出队并输出、(3)计算队中元素个数并输出。

要求：

输入格式
      第一行一个数字N
      下面N行,每行第一个数字为操作命令(1)入队、(2)出队并输出、(3)计算队中元素个数并输出。
输出格式
      若干行每行显示一个2或3命令的输出结果。注意: 2.出队命令可能会出现空队出队(下溢) ,请输出"no",并退出。
           数据规模和约定
           1≤N≤5
输入样例：
7
1   19
1   56
2
3
2
3
2
输出样例：
19
1
56
0
no

--------------------------------------------------------
程序如下：
#include<stdio.h>
int main()
{
	int n,s,i,j=0,a[100];
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&s);
		switch(s)
		{
			case 1:
				scanf("%d",&a[j]);
				j++;
				break;
			case 2:
				if(s==2&&j==0)
		            j--;
				if(j<0)
				{
					printf("no\n");
					return 0;
				}
				else
				{
					printf("%d\n",a[0]);
				    for(i=0;i<j;i++)
				   {
					    a[i]=a[i+1];
				   }
				   j--;
				}
				break;
			case 3:
				printf("%d\n",j);
				break;
			default:
				break;
		}
	}
	return 0;
}
